{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceRoot}/target/debug/HyNodeEditor",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "rust: cargo build"
        }
    ]
}